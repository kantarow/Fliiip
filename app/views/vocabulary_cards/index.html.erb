<%= link_to "Create new vocabulary card", new_vocabulary_card_path, class: "responsive-btn btn-normal" %>
<% cards.each do |card| %>
  <%= link_to card, class: "card-headline" do %>
    <%= "#{card.en} / #{card.ja}" %>
  <% end %>
<% end %>
